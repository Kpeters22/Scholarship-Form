# Scholarship-Form
A PHP web application that collects user information through an HTML form and processes the submitted data to a PHP file using the POST method. PHP extracts the form data and displays a personalized confirmation message.
